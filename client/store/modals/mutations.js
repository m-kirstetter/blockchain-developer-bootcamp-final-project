const ModalsMutations = {
  SET_SUBMITWORK_MODAL(state, data) {
    state.submitWork = data;
  },
  SET_POSTGIG_MODAL(state, data) {
    state.postGig = data;
  },
};

export default ModalsMutations;
